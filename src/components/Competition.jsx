import { Typography } from "@mui/material";

function Competition() {
  return (
    <Typography variant="h3">
      Competition should be shown here, with an input so that we can filter
    </Typography>
  );
}

export default Competition;
